import React from 'react';
import { Box, Typography } from '@mui/material';

export default function OrdiniFornitoreManager() {
  return (
    <Box>
      <Typography variant="h4">Ordini Fornitori</Typography>
      <Typography>Componente in sviluppo</Typography>
    </Box>
  );
}