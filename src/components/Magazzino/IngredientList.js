import React from 'react';
import { Box, Typography } from '@mui/material';

export default function IngredientList() {
  return (
    <Box>
      <Typography variant="h4">Lista Ingredienti</Typography>
      <Typography>Componente in sviluppo</Typography>
    </Box>
  );
}