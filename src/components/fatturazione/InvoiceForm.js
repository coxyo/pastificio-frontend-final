import React from 'react';
import { Box, Typography } from '@mui/material';

export default function InvoiceForm() {
  return (
    <Box>
      <Typography variant="h4">Nuova Fattura</Typography>
      <Typography>Form fattura in sviluppo</Typography>
    </Box>
  );
}