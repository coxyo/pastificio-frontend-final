import React from 'react';
import { Box, Typography } from '@mui/material';

export default function RegistroFatture() {
  return (
    <Box>
      <Typography variant="h4">Registro Fatture</Typography>
      <Typography>Registro in sviluppo</Typography>
    </Box>
  );
}